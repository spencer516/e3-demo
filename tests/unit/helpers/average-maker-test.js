import { averageMaker } from '../../../helpers/average-maker';
import { module, test } from 'qunit';

module('Unit | Helper | average maker');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = averageMaker(42);
  assert.ok(result);
});
